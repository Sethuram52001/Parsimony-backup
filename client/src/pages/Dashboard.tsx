import * as React from 'react';
import { Transactions } from '../components';

const Dashboard = () => {
  return (
    <React.Fragment>
      <h1>Dashboard</h1>
      <Transactions />
    </React.Fragment>
  );
};

export default Dashboard;
